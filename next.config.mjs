const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "skilledupload-bucket.nyc3.digitaloceanspaces.com",
      "as1.ftcdn.net",
    ],
  },
  distDir: "build",
};

export default nextConfig;
